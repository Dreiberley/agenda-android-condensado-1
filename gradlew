#!/bin/sh

# [Conteúdo completo do script gradlew como fornecido na mensagem anterior - copie da resposta histórica ou do passo anterior]
# Nota: Para brevidade, use o conteúdo do gradlew que eu dei antes. Se precisar, cole o script inteiro aqui (é longo, mas essencial).
